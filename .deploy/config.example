# Deploy configuration
# ---------------------
# Copy this file to ".deploy/config" and fill in your values.
# The "config" file is gitignored and never committed.

# Project title (display only)
PROJECT="My Kirby Site"

# Expected local folder name (safety guard: deploy only runs from this folder)
LOCAL="my-project"

# SSH port flag, e.g. "-p22" or "-p2121"
PORT="-p22"

# Local root folder to sync from (trailing slash = sync contents)
FOLDER="./"

# --- Profiles -------------------------------------------------------------
# One server + target per profile. Add or remove profiles freely.
#   <name>_SERVER   user@host
#   <name>_TARGET   remote path (trailing slash matters)

LIVE_SERVER="user@example.com"
LIVE_TARGET="/var/www/vhosts/example.com/"

STAGING_SERVER="user@example.com"
STAGING_TARGET="/var/www/vhosts/staging.example.com/"
STAGING_PORT="-p2121"
